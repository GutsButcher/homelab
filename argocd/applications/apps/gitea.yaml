apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: gitea
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "0"
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: homelab
  destination:
    server: https://kubernetes.default.svc
    namespace: gitea
  sources:
    # Git repo referenced only so the Helm source below can read $values/.
    - repoURL: https://github.com/GutsButcher/homelab.git
      targetRevision: main
      ref: values
    # Namespace, secrets and Traefik routes that live alongside the chart.
    - repoURL: https://github.com/GutsButcher/homelab.git
      targetRevision: main
      path: apps/gitea/prod/gitea
    - repoURL: https://dl.gitea.com/charts
      chart: gitea
      targetRevision: 12.5.0
      helm:
        releaseName: gitea
        valueFiles:
          - $values/apps/gitea/values.yaml
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      # Apply server-side: several charts here ship CRDs too large for the
      # client-side last-applied-configuration annotation.
      - ServerSideApply=true
      - RespectIgnoreDifferences=true
    retry:
      limit: 5
      backoff:
        duration: 30s
        factor: 2
        maxDuration: 10m
  ignoreDifferences:
    # The bitnami postgresql subchart generates this password with randAlphaNum and
    # normally keeps it stable by `lookup`-ing the existing Secret. Argo CD's repo-server
    # renders without cluster access, so lookup returns nothing and a fresh password is
    # produced on every render - permanently OutOfSync, and a sync would rotate the
    # password out from under the running gitea. Combined with RespectIgnoreDifferences
    # above, this leaves the live Secret alone.
    - group: ""
      kind: Secret
      name: gitea-postgresql
      namespace: gitea
      jsonPointers:
        - /data
