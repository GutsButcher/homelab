apiVersion: source.toolkit.fluxcd.io/v1beta2
kind: HelmRepository # resource that defines a Helm repository source
metadata:
  name: jenkins
  namespace: flux-system
spec:
  interval: 24h
  url: https://charts.jenkins.io # URL of the Helm repository source