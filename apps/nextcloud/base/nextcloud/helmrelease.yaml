apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: authentik
  namespace: authentik
spec:
  interval: 30m
  chart:
    spec:
      chart: authentik
      version: ">=2025.8.3 <2026.0.0"
      sourceRef:
        kind: HelmRepository
        name: goauthentik # the Chart is jameswynn/homepage
        namespace: flux-system
