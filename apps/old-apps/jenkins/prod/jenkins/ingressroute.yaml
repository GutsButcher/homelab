apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: jenkins
  namespace: jenkins
spec:
  entryPoints:
    - web
  routes:
    - match: Host(`jenkins.gwynbliedd.com`)
      kind: Rule
      services:
        - name: jenkins
          namespace: jenkins
          port: 8080
