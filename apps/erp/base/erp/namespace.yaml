apiVersion: v1
kind: Namespace
metadata:
  name: erp
  annotations:
    # Earliest wave: everything else in this application is created inside it,
    # including the wave -2 bootstrap identity.
    argocd.argoproj.io/sync-wave: "-3"
  labels:
    app.kubernetes.io/name: erp
    app.kubernetes.io/part-of: homelab
